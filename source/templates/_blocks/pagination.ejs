<%
  var pagination = { prev: '', next: '' }
%>
<div class="row flex-edges">
  <% if (pagination.prev) { %>
    <a href="<%= pagination.prev %>" class="paper-btn">上一页</a>
  <% } %>
  <% if (pagination.next) { %>
    <a href="<%= pagination.next %>" class="paper-btn">下一页</a>
  <% } %>
</div>
